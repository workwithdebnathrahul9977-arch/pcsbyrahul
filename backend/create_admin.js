const { PrismaClient } = require("@prisma/client"); const bcrypt = require("bcryptjs"); const prisma = new PrismaClient(); async function main() { const email = "admin@physchemia.com"; const password = "admin"; const hashedPassword = await bcrypt.hash(password, 10); const existing = await prisma.user.findUnique({ where: { email } }); if (existing) { await prisma.user.update({ where: { email }, data: { role: "ADMIN", password: hashedPassword } }); console.log("Admin user updated."); } else { await prisma.user.create({ data: { name: "Admin", email, password: hashedPassword, phone: "01700000000", role: "ADMIN" } }); console.log("Admin user created."); } } main().catch(console.error).finally(() => prisma.$disconnect() );
