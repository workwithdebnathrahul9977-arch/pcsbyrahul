const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file = file = path.resolve(dir, file); const stat = fs.statSync(file); if (stat 
